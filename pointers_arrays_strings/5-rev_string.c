#include "main.h"

/**
 * rev_string - Check the code.
 * Description: 'Reverses a string.'
 * @s: is a string.
 */

void rev_string(char *s)
{
	int i;
	char k;

	for (i = 0; *(s + i) != '\0'; i++)
	{
	}
	i--;
}
