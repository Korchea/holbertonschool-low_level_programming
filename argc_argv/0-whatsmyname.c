#include <stdio.h>

/**
 * main - Check the code.
 * Description: 'Print the file name.'
 * @i: is an integer.
 * @argv: is the file name.
 * Return: 0.
 */

int main(int i, char *argv[])
{
	i = 0;
	printf("%s\n", argv[0]);
	return (i);
}
