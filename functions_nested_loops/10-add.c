#include "main.h"

/**
 * add - Entry point
 * Description: 'Adds two integers and returns the result.'
 * @i: is an int
 * @j: is an int
 * Return: Always i + j (Success)
 */

int add(int i, int j)
{
	return (i + j);
}
